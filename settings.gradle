pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        //asksira loopviewpager 사용 위함
        maven { url "https://www.jitpack.io" }
        jcenter()
    }
}
rootProject.name = "Ddokddac"
include ':app'
